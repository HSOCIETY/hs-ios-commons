#
# Be sure to run `pod lib lint HSSwiftCommon.podspec' to ensure this is a
# valid spec before submitting.
#
# Any lines starting with a # are optional, but their use is encouraged
# To learn more about a Podspec see http://guides.cocoapods.org/syntax/podspec.html
#

Pod::Spec.new do |s|
    s.name             = 'HSSwiftCommon'
    s.version          = '0.1.18'
    s.summary          = 'HSOCIETY Common Lib'
    
    # This description is used to generate tags and improve search results.
    #   * Think: What does it do? Why did you write it? What is the focus?
    #   * Try to keep it short, snappy and to the point.
    #   * Write the description between the DESC delimiters below.
    #   * Finally, don't worry about the indent, CocoaPods strips it!
    
    s.description      = 'HSOCIETY Swift 공통코드 모음'
    
    s.homepage         = 'https://github.com/HSOCIETY/hs-ios-commons.git'
    # s.screenshots     = 'www.example.com/screenshots_1', 'www.example.com/screenshots_2'
    s.license          = { :type => 'MIT', :file => 'LICENSE' }
    s.author           = { 'HSOCIETY iOS Developer' => 'yoseop.park@hsociety.co.kr' }
    s.source           = { :git => 'https://github.com/HSOCIETY/hs-ios-commons.git', :tag => s.version.to_s }
    
    s.ios.deployment_target = '9.0'
    s.requires_arc = true
#    s.source_files = 'HSSwiftCommon/Classes/Commons.swift', 'HSSwiftCommon/Classes/UIScreen_Extension.swift'
    s.source_files = 'HSSwiftCommon/Classes/*'

    # s.resource_bundles = {
    #     'HSSwiftCommon' => ['HSSwiftCommon/Assets/*.png']
    # }
    s.swift_version = '4.0'
    # s.public_header_files = 'Pod/Classes/**/*.h'
    # s.frameworks = 'UIKit', 'MapKit'
    # s.dependency 'AFNetworking', '~> 2.3'
end


